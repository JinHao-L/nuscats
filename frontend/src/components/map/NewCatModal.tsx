import { CatSighting } from '@api/sightings';
import React from 'react';

type NewCatModalProps = {
  sighting: CatSighting;
};

const NewCatModal = ({ sighting }: NewCatModalProps) => {
  return <div></div>;
};

export default NewCatModal;
